 <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>

          <!-- <a class="navbar-brand text-uppercase" href="#">
            My contact
          </a> -->
          <%= link_to "Mis Contactos", root_path, class: "navbar-brand text-uppercase" %>
        </div>
        <!-- /.navbar-header -->
        <div class="collapse navbar-collapse" id="navbar-collapse">

          <% if !user_signed_in? %>
            <div class="navbar-right">
              <!-- <a href="#" class="btn btn-default navbar-btn">Login</a> -->
              <!-- <a href="#" class="btn btn-primary navbar-btn">Register</a> -->
              <%= link_to "Login", new_user_session_path, class: "btn btn-default navbar-btn" %>
              <%= link_to "Register", new_user_registration_path, class: "btn btn-primary navbar-btn" %>

            </div>
          <% else %>
            <ul class="nav navbar-nav">
                <li class="<%= active_item('dashboard') %>"><%= link_to "Dashboard", dashboard_index_path %></li>
                <li class="<%= active_item('contacts') %>"><%= link_to "All Contacts", contacts_path %></li>
                <li class="<%= active_item('tasks') %>"><%= link_to "My Tasks", tasks_path %></li>
            </ul>
            <ul class="nav navbar-right navbar-nav">
              <li class="dropdown">
                  <!-- <li class="nav-item dropdown"> -->
                   <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                     <span class="glyphicon glyphicon-user"></span>
                     <%= current_user.name %>
                      <span class="caret"></span>
                   </a>
                <ul class="dropdown-menu" aria-labelledby="dropdownMenu1">
                  <li>
                      <!-- <a class="dropdown-item" href="#">Profile</a> -->
                      <%= link_to "Profile", edit_user_registration_path %>
                    </li>
                  <li>
                      <!-- <a class="dropdown-item" href="#">Logout</a> -->
                      <%= link_to "Logout", destroy_user_session_path, method: :delete %>
                    </li>

                </ul>
                <!-- </li> -->
              </li>
            </ul>
            <%= render 'layouts/search_form' %>
          <% end %>

        </div>
      </div>
  </nav>
